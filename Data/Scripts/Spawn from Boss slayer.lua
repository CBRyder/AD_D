local propSpawnpoint_1 = script:GetCustomProperty("SpawnPoint"):WaitForObject(player)
local propSpawnPoint = script:GetCustomProperty("SpawnPoint"):WaitForObject(player)

if player.spawn 
	then spawn=propspawnpoint_1
	
	else spawn=propSpawnPoint
end
